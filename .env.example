HOST=127.0.0.1
PORT=8787
MCP_PATH=/mcp
VAULT_PATH=/path/to/vault
VAULT_NAME=Obsidian
READ_ONLY=true

# Set these in production when the service is behind Cloudflare Access.
# With Managed OAuth, Cloudflare handles the OAuth flow before requests
# reach this origin. The origin only validates Cloudflare's assertion.
CF_ACCESS_REQUIRED=true
CF_ACCESS_TEAM_DOMAIN=https://your-team.cloudflareaccess.com
CF_ACCESS_AUD=your-access-application-aud
CF_ACCESS_ALLOWED_EMAILS=you@example.com

# Optional browser origin guard. Non-browser MCP clients often omit Origin.
ALLOWED_ORIGINS=https://claude.ai,https://chatgpt.com,https://chat.openai.com

# Optional Host allow-list for DNS-rebinding protection. Must match the exact
# Host header the origin receives (include the port if non-standard). When set,
# requests with any other Host are rejected. Leave empty to disable.
# ALLOWED_HOSTS=obsidian-mcp.example.com,127.0.0.1:8787,localhost:8787

# Per-client request rate limit (requests/minute, keyed by cf-connecting-ip).
# 0 disables. Cloudflare's own rate limiting should be the primary control.
RATE_LIMIT_PER_MINUTE=300
